<div class="align">
<h1>New Listing</h1>
</div>
<div class="align">
    <% if params[:motorhome_id] == nil%>
        <%= render partial: 'form', locals: {listing: @listing} %>
    <% else %>
        <%= render partial: 'form', locals: {listing: @listing, motorhome_id: params[:motorhome_id]} %>
    <% end %>
</div>